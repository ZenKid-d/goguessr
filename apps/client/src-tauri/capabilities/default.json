{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. App-defined commands (load_settings, save_settings, add_record, list_records, best_score) are allowed to the app's own windows without extra permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
